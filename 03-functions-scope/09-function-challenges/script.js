// const getCelsius = fahrenheit => (fahrenheit - 32) * 5 / 9;

// console.log(`The temp is ${getCelsius(50)} degree celcius`)

// console.log(getCelsius(62));

/////task2
// const minMax = arr1 => {
//  console.log( ...arr1);
//   return { max: Math.max(...arr1), min: Math.min(...arr1) };
// };

// console.log(minMax([1, 4, 8]));

(function (length, width) {
  console.log(
    `The area of a rectangle with a length of ${length} and a width of ${width} is  ${
      length * width
    }.`,
  );
})(10, 5);
